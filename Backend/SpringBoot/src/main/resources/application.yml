spring:
  profiles:
#    active: ${SPRING_PROFILES_ACTIVE:local-h2-mem-with-liquibase}
#    active: ${SPRING_PROFILES_ACTIVE:local-h2-file}
#    active: ${SPRING_PROFILES_ACTIVE:local-h2-file-with-liquibase}
    active: ${SPRING_PROFILES_ACTIVE:local-docker-mysql-file}